{{#if images.[0]}}
<img src="/api/images/{{ images.0.id }}">
{{/if}}
<h2><a href="/product/{{id}}">{{ name }} - Shipping Cost: ${{ price }}</a></h2>
<div class="handle-user">
<p>Posted by {{ user.username }}</p>
</div>